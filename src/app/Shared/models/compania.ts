//Interface que permite la lectura de la información de la compañía
export interface Compania {
    direccion: string,
    nitCompania: number,
    razonSocial: string,
    telefonoCompania: string,
}
